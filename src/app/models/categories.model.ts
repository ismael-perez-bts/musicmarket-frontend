/**
 * Category interface for items and search filters.
 */
export interface Category {
  /**
   * Id of category
   */
  id: number;
  /**
   * Category internal name
   */
  category: string;
  /**
   * category label
   */
  label: string;
}
